# MAIS-NATURAL-CA60-V5-R11 — Fresh A11 Independent Offline Runner Review

## Decision

`CONCURRED`

The exact V5-R11 source, registration, A07 closeout, production/test/import
closures, frozen research bindings, native reference workflow, provider guards,
statistical kernel, decision ceiling, recovery custody, and review-process
schemas were independently reviewed offline. No actionable finding remains.

This is a runner-registration review decision only. It does not authorize or
prove provider execution, credentials, egress, route/account/billing/region
facts, tokens, attempts, USD, reference labels, natural-question results,
deployment, or generalized machine-QA effectiveness.

Reviewed immutable identities:

- Exact A07 closeout and review-commit parent:
  `e46d2bb8cf62c23f62816634b0dca778a368e35a`.
- Exact registration commit:
  `7916b771ab29c8ccb13013d52041c3675c8cf7e4`.
- Exact direct-parent source commit:
  `2eb803261e97a2add469182d193533f4751fbfa5`.
- Registration self-hash:
  `8ad0c241b7da6c738740e4de074a92805ac45823f63139b23ef327181874817c`.
- Production source root:
  `52400f9d4979ac88856c25ea7965de19afa25bfa36da53aab3937445d5851f31`.
- Test source root:
  `eb4b2b89b98968d94d8bdbac876086f85485ff26ad3cef46afd1a90c8a722a3b`.
- Import-closure root:
  `17f1764b6301935a1bb86e2622329f626a359c5afa836e035095fd2309d9221e`.
- Runtime source-enumeration root:
  `43dd97b822ea191cb314f7b50e60d39df5316e2682dd546ac8697228aa4997f6`.
- A07 closeout self-hash:
  `92bde7669e099399a3f483311555b5a568cce547cbb8cb6809f50fb8dbfa8814`.
- Signed A11 review receipt self-hash:
  `55791f1df10ec776c4cfba59ae2ef4abec7f32ba573bb1c92852e0930a3a76cc`.
- Process-evidence root:
  `ba5232e7d2fda745b96b96c834b74cf7e9655d589792791147564968cae8d80c`.

The registered provider tuples remain:

- OpenAI reference:
  `OPENAI_DIRECT / gpt-5.6-luna / https://us.api.openai.com/v1/responses / US_STORAGE_PROCESSING`.
- DeepSeek evaluation:
  `DEEPSEEK_DIRECT / deepseek-v4-pro / https://api.deepseek.com/chat/completions / UNRESOLVED`.

The registration contains no trusted provider-evidence anchor, no credential or
provider authority, and no token/attempt/USD grant. Its route state remains
`ROUTE_AUTHENTICITY_BLOCKED_NO_PINNED_TRUST_ANCHOR`; therefore concurrence does
not make either provider path executable.

## Findings

No actionable findings.

- Critical: `0`.
- High: `0`.
- Medium: `0`.
- Low: `0`.
- Requires a superseding registration because of this review: `false`.

The self-hashed finding ledger is
`a8dc77e89dba39bfceed8b0a60ef37fb17bdbb089bd5a8a7fee6117073ab6d18`.

## Closure matrix

| Prior finding | Independent R11 disposition | Evidence |
| --- | --- | --- |
| `A11-R8-001` | `CLOSED` | The default native runtime rebuilds 240 raw OpenAI fixture responses, emits closed `MachineReferenceSealV7` and `ReferenceSealValidationReceiptV5` artifacts, then freezes `DeepSeekExecutionRegistrationV4`. |
| `A11-R8-002` | `CLOSED` | The final adapter reloads the exact registration, A07 closeout, signed A11 review and pinned identity from immutable Git objects before credential read or fetch. |
| `A11-R8-003` | `CLOSED` | Route validation reconstructs retained structured account/billing/region/price source bytes and the exact ten-artifact zero-natural-content probe graph, and requires a pinned Ed25519 trust anchor. The current no-anchor state fails closed. |
| `A11-R8-004` | `CLOSED` | Terminal completeness and failure evidence are rebuilt from raw attempt, ledger, journal, marker, inventory and item custody. |
| `A11-R8-005` | `CLOSED` | Decision-critical integrity facts are reconstructed from raw custody; caller-authored favorable or adverse flags do not change the result. |
| `A11-R8-006` | `CLOSED` | Interrupted recovery reloads protected custody, rejects orphan/missing-prepared evidence, forbids transport and credential dependencies, and remains zero-HTTP. |
| `A11-R8-007` | `CLOSED` | The finding identity is item-scoped (`itemId`, `findingId`, family, code), and exact-first then family one-to-one matching is deterministic. |
| `A11-R8-008` | `CLOSED` | The A07 receipt and session log were single-added together at the exact direct child of registration and bind roots, 52/0/0, clean state and `REVIEWED_COMMIT`. |
| `A11-R9-001` | `CLOSED` | Pre-review phase-order failure is accepted only as the expected missing-fresh-review stop while credential and HTTP counts remain zero. |
| `A11-R10-001` | `CLOSED` | The R11 native builders and closed schemas share the exact R11-bound seal and validation status enums; the full native 240-response test reaches execution freeze. |
| `A11-R10-002` | `CLOSED` | New V2 process schemas bind `runnerVersion = V5-R11`; the review receipt is V9 and its signed payload is V3. |

## Independent recomputation

The A11 verifier uses Node built-ins and immutable Git objects. It does not
import the A07/A21 registration builder, runner, activation guard, primary
scorer, statistical kernel, decision engine, or provider adapter as its
independent implementation.

It independently established:

- `242` production paths and `509` relative-import edges;
- `274` registered test paths and `717` relative-import edges;
- exact production/test manifests, byte lengths, SHA-256 values and roots;
- exact source → registration → closeout direct-parent history;
- immutable single-add registration and closeout custody;
- exact R10 signed `DISCREPANCY` predecessor chain and Ed25519 verification;
- exact R11 identity-anchor fingerprint and registration binding;
- exact provider tuples, frozen design/frame/sample/rights/privacy/taxonomy/
  threshold/P0-P2/decision-ceiling bindings and zero authority; and
- exact R11 native V7/V5 schema/status/runtime reachability.

Verifier result: `16 verified / 0 mismatches`.

The A11 adversarial suite independently implemented Wilson intervals,
counter-SHA256 bounded draws, one-to-one exact-first matching and claim-ceiling
assertions. It also inspected route-source reconstruction, final-adapter order,
recovery, unified nonresolved accounting, CLI reachability and zero authority.
It imports none of the primary scorer, statistical kernel or decision engine.

Adversarial result: `15 passed / 0 failed / 0 skipped`.

Fresh exact 14-entrypoint registered offline suite:
`52 passed / 0 failed / 0 skipped / 0 cancelled / 0 todo`, duration
`2,436,046.252458 ms`. Its native 240-response test passed in
`2,395,226.25525 ms`.

The offline suite's 240 retained fixture attempts are synthetic test evidence,
not live provider attempts and not natural-question results.

## Review process and signature evidence

All seven loader-required paths are added together in the single A11 review
commit whose direct parent is the exact A07 closeout:

- `independent-runner-review-receipt.json`;
- `verifier-source-manifest.json`;
- `dependency-lock-manifest.json`;
- `independent-static-import-graph-receipt.json`;
- `forbidden-primary-scorer-path-scan-receipt.json`;
- `independent-command-runtime-receipt.json`; and
- `independent-source-enumeration-receipt.json`.

Process identities:

- Verifier-source tree root:
  `d9217e6d70a2dd528cfacedfe40fd10ee691132155ef6ac9360748d8d6998db4`.
- Dependency-lock root:
  `4a0d2a884e0d7141da1da021073dbd48aba5c699bba753c60f399469b3b67fc6`.
- Static-import graph receipt:
  `787e2cf3978ff608d6cec09680dd7856ea559bed149be0ed5149530858b8d142`
  (`17` import edges, `0` forbidden primary imports).
- Forbidden executable-import scan receipt:
  `03bf494effdec29e961226a1177eeec89fe4658279c4f787aa95ed66f248bd17`
  (`3` independent sources, `0` forbidden matches).
- Command-runtime receipt:
  `ff9785367512233441b9238c31bd99e537a07edd680e0c67a4e1875753f4be9a`.
- Independent enumeration receipt:
  `d1bbec384d06912086375c987080ac1a9cb0780fa6c5bab9373c8930ff5d7e13`
  (`242` paths, `EXACT_MATCH`).
- Review signature payload hash:
  `6c0c565e14a3ac64a144559e457ba068992e4643da9e4f8e421e319ce3ae896d`.

The one-purpose R11 private Ed25519 key remained in protected local storage at
mode `0600`. It was read programmatically only for this public review signature;
its bytes and exact path were never printed, copied, committed or returned. The
signature verifies against the public identity pinned by the registration.

## Proved boundary and activity accounting

- Provider credential/environment/DOCX reads: `0`.
- Protected natural-question artifact or natural-question body reads: `0`.
- OpenAI/DeepSeek/provider API/HTTP/route-probe calls: `0`.
- Natural-question egress: `0`.
- Live provider tokens/attempts/USD authorized or consumed: `0 / 0 / 0`.
- Reference labels/natural-question results produced: `0 / 0`.
- Live question bank, app, API, deployment, source, registration and closeout
  mutations: `0`.

The separately authorized Git push carries review evidence only. It is not a
provider call or natural-question egress.

The California-60 decision and claim ceilings remain unchanged. This review
does not permit `PASS`, `APPROVED`, `PRODUCTION_READY`, or
`LIMITED_GENERALIZATION_EVIDENCE`; even after a valid complete CA60 execution,
the normal maximum remains `INCONCLUSIVE_MACHINE_REFERENCE`.
