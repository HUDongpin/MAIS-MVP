# S25 Data 28 Release-Intake Slice

Date: 2026-06-20
Session: S25 git hygiene and release intake lead
Scope: non-destructive dirty-tree slicing for the 28 untracked files under `data/`

## Summary

`git status --porcelain -- data` currently shows 67 dirty `data/` entries:

- 39 modified tracked files.
- 28 untracked files.

This report slices only the 28 untracked `data/` files requested by the owner. The 39 modified tracked files should be handled in a separate release-intake pass because they include shared live surfaces such as `data/questions.ts`, `data/topics.ts`, `data/grades.ts`, `data/lessons.ts`, `data/visualizationLabs.ts`, existing mainland data, and existing `data/rag/*`.

No files in `data/` were edited, moved, staged, deleted, reverted, or normalized by S25.

## Blocking Observations

1. Several untracked adapter files import `data/generated-content/...` packages that are not present in the current visible `data/generated-content/` tree. These slices cannot be landed as adapter-only commits without either adding the approved source packages under an owner-assigned S21 scope or changing the adapter source path under the owning feature/content scope.
2. `data/rag/*` is a shared live RAG surface. The five untracked Hong Kong RAG files must stay in a separate S21/S18/S23 gate and must not be bundled with practice, lesson, game, or runtime seed data.
3. `data/forum.ts` is runtime/community seed data, not curriculum content. It is referenced by `lib/server/userStore.ts` and forum API coverage, so it needs backend/runtime ownership rather than S18 curriculum QA alone.
4. `data/gameBasedLearning.ts`, `data/mathVirusBlaster.ts`, and `data/mightyTankBattle.ts` are S20 game-data surfaces. They should not be bundled with reward economy (`data/gamification.ts`) or curriculum bank changes.
5. `data/usMathTopics.ts` depends on the already modified tracked `data/rag/usMath.ts`; the US core math slice has a tracked-data dependency outside this 28-file package.

## Recommended Slices

### Slice A: California / US_CA_MATH

Count: 7
Primary gate: S18 curriculum QA, S21 content-pipeline source package handling, S23 promotion planning
Live integration owners after approval: S03 roadmap/topics, S04 questions, S05 lessons/textbook, S11 regression, S22 release readiness

Files:

- `data/usCaliforniaHighSchoolLessonIllustrations.ts`
- `data/usCaliforniaKnowledgePoints.ts`
- `data/usCaliforniaLessons.test.ts`
- `data/usCaliforniaLessons.ts`
- `data/usCaliforniaMicroLessons.ts`
- `data/usCaliforniaQuestions.ts`
- `data/usCaliforniaTopics.ts`

Dependency notes:

- `data/usCaliforniaLessons.ts` imports missing `data/generated-content/us-ca-math-k-g5-textbooks-v1/lessons.json`.
- `data/usCaliforniaTopics.ts` imports missing `data/generated-content/us-ca-math-g6-g12-generated-bank-v2-1500/question-pack.json`, missing `data/generated-content/us-ca-math-k-g5-generated-bank-v3-deepseek-1500/question-pack.json`, and missing `data/generated-content/us-ca-math-k-g5-textbooks-v1/lessons.json`.
- `data/usCaliforniaQuestions.ts` depends on the generated topic/question objects from `data/usCaliforniaTopics.ts`.
- `data/usCaliforniaLessons.test.ts` belongs with this slice, but S11 should decide whether this test remains in `data/` or moves into the broader test ownership plan.

Recommended handling:

- Do not land adapter files until S21 confirms the source packages are present, approved, and safe for the target path.
- If a smaller cut is needed, split California into `CA-K5/generated/adaptive`, `CA-G6-G12 question bank`, and `CA-high-school textbook illustration` packages.

### Slice B: Arkansas / US_AR_MATH

Count: 4
Primary gate: S18 curriculum QA, S21 source package handling, S23 promotion planning
Live integration owners after approval: S03 topics, S04 questions, S05 lessons, S11 regression, S22 release readiness

Files:

- `data/usArkansasMiddleSchoolLessonIllustrations.ts`
- `data/usArkansasMiddleSchoolLessons.ts`
- `data/usArkansasQuestions.ts`
- `data/usArkansasTopics.ts`

Dependency notes:

- `data/usArkansasMiddleSchoolLessons.ts` imports missing `data/generated-content/us-ar-math-textbooks-v1/textbook-pack.json`.
- `data/usArkansasQuestions.ts` and `data/usArkansasTopics.ts` import missing Arkansas K-G5 and G6-G12 generated question packages under `data/generated-content/`.
- This slice is referenced by `lib/mvpReadiness.test.ts`, `lib/questionBankSolvability.ts`, and `components/lesson/LessonView.tsx`.

Recommended handling:

- Keep Arkansas separate from California and Florida even when shared US visualization/test surfaces mention all three.
- Land only with the source packages and a targeted S11/S22 check plan.

### Slice C: Florida / US_FL_MATH

Count: 3
Primary gate: S18 curriculum QA, S21 source package handling, S23 promotion planning
Live integration owners after approval: S03 topics, S04 questions, S05 lessons, S11 regression, S22 release readiness

Files:

- `data/usFloridaMiddleSchoolLessons.ts`
- `data/usFloridaMiddleSchoolQuestions.ts`
- `data/usFloridaMiddleSchoolTopics.ts`

Dependency notes:

- `data/usFloridaMiddleSchoolTopics.ts` imports missing `data/generated-content/us-fl-math-middle-school-textbooks-v1/textbook-pack.json`.
- This slice is referenced by `lib/mvpReadiness.test.ts`, `lib/questionBankSolvability.ts`, and existing mainland/high-school cross-route tests.

Recommended handling:

- Keep this as a compact Florida middle-school package.
- Do not bundle with generic US math or Arkansas/California checks except in a final combined US regression pass.

### Slice D: Generic US Math Live Topics

Count: 2
Primary gate: S18 US math QA, S23 live promotion planning
Integration owners after approval: S03 topics, S04 questions, S11 regression, S22 release readiness

Files:

- `data/usMathQuestions.ts`
- `data/usMathTopics.ts`

Dependency notes:

- `data/usMathTopics.ts` imports `data/rag/usMath.ts`, which is already modified and outside this 28-file untracked set.
- `data/topics.ts` imports `usMathLiveTopics`, and `data/questions.ts` imports `usMathLiveQuestions`.

Recommended handling:

- Treat this as a separate US core/rag-derived live package.
- Do not land before the modified tracked `data/rag/usMath.ts`, `data/topics.ts`, and `data/questions.ts` changes are reviewed in the separate tracked-data slice.

### Slice E: Hong Kong EASE Practice Bank

Count: 1
Primary gate: S18 Hong Kong content QA
Integration owners after approval: S04 practice, S11 solvability/regression, S22 release readiness

Files:

- `data/hongKongEasePracticeQuestions.ts`

Dependency notes:

- Imports missing `data/generated-content/hk-ease-practice-bank-v1/question-pack.json`.
- Referenced by `lib/fullQuestionBankSolvability.test.ts` and `lib/questionBankSolvability.ts`.

Recommended handling:

- Keep live practice adapter separate from the Hong Kong RAG safe-card slice.
- S21 must provide or confirm the approved generated source package before S04/S18/S11 can validate this adapter.

### Slice F: Hong Kong RAG Safe Cards

Count: 5
Primary gate: S21 RAG intake, S18 source-distance/curriculum QA, S23 promotion planning
Integration owners after approval: RAG wrappers/tests, S11 regression, S22 release readiness

Files:

- `data/rag/hongKongEaseQuestions.ts`
- `data/rag/hongKongModernPrimary.ts`
- `data/rag/hongKongUpJunior.ts`
- `data/rag/hongKongUpJuniorEnglish.ts`
- `data/rag/hongKongUpJuniorResources.ts`

Dependency notes:

- These are imported by `lib/rag/hongKongEaseQuestions.ts`, `lib/rag/hongKongModernPrimary.ts`, `lib/rag/hongKongUpJunior.ts`, and `lib/rag/hongKongUpJuniorResources.ts`.
- Existing RAG tests assert safe-card counts and raw-source guardrails.

Recommended handling:

- Package all five together only if they came from the same HK RAG intake/review batch.
- Run RAG-specific safety checks before any promotion. Per the MAIS RAG intake workflow, production use remains blocked until S18/S23 approval is recorded.

### Slice G: Mainland BNU Lessons

Count: 2
Primary gate: S18 mainland BNU content QA, S21 source package handling, S23 promotion planning
Live integration owner after approval: S05 lessons, S11 regression, S22 release readiness

Files:

- `data/mainlandBnuHighLessons.ts`
- `data/mainlandBnuJuniorLessons.ts`

Dependency notes:

- `data/mainlandBnuHighLessons.ts` derives lesson seeds from existing BNU high questions/topics and localization helpers.
- `data/mainlandBnuJuniorLessons.ts` imports missing `data/generated-content/mainland-bnu-junior-lessons-v1/lessons.json`.
- A prior blocker references the missing BNU junior lessons package under `coordination/content-qa/mainland-bnu-junior-lessons-v1/lessons.json`.

Recommended handling:

- Split BNU high and BNU junior if the junior source package remains missing.
- Do not bundle with PEP/HJB tracked data changes.

### Slice H: Game Data Runtime

Count: 3
Primary owner: S20 game design and game-based learning lead
Required coordination: S17 only if reward economy is touched, S12 if game storage/API is touched, S04/S18 for embedded math question quality, S11 tests, S22 release readiness

Files:

- `data/gameBasedLearning.ts`
- `data/mathVirusBlaster.ts`
- `data/mightyTankBattle.ts`

Dependency notes:

- Referenced by `components/games/*`, `lib/gameBasedLearning.ts`, `lib/mathVirusBlaster.ts`, `lib/mightyTankBattle.ts`, and related tests.
- This is runtime game design data, not curriculum bank promotion data.

Recommended handling:

- Keep as a S20 game package.
- Do not bundle with `data/gamification.ts`, which is S17-owned and already tracked/modified separately.

### Slice I: Forum Runtime Seed

Count: 1
Primary owner: S12 backend/API platform for runtime seed and storage behavior
Required coordination: forum/product surface owner if one is assigned, S11 API coverage, S22 release readiness

Files:

- `data/forum.ts`

Dependency notes:

- Referenced by `lib/server/userStore.ts` and `tests/e2e/class-forum.spec.ts`.
- This is not curriculum, RAG, lesson, or game data.

Recommended handling:

- Keep isolated as a runtime seed package.
- Do not bundle with classroom/teacher UI, curriculum, or content QA slices.

## Suggested Order

1. S21/S18/S23 resolve source-package availability for missing generated-content dependencies.
2. S20 handles game data as an independent runtime package.
3. S12 handles forum runtime seed independently.
4. S21/S18/S23 handle Hong Kong RAG safe cards with RAG safety scans.
5. S18/S23 route region-specific US curriculum packages in this order: Florida, Arkansas, California, then generic US core after `data/rag/usMath.ts` is reviewed.
6. S05/S18/S23 handle BNU high first if desired; hold BNU junior until the missing junior lesson package is resolved.

## Checks Run

- `git status --short`
- `git status --porcelain=v1 -- data`
- `git diff --name-status -- data`
- `git ls-files --others --exclude-standard -- data`
- `find data -maxdepth 2 -type f`
- Targeted `rg` dependency checks for untracked data file basenames.
- Existence checks for referenced `data/generated-content/...` packages.
- Post-write coverage check confirmed all 28 untracked `data/` file paths are listed in this report.

## Checks Not Run

- No type-check/build/test commands were run because S25 did not change runtime code and several adapter dependencies are missing from the visible working tree.
- No RAG safety scan was run because S25 did not produce or modify RAG artifacts; that belongs to S21/S18/S23 for the RAG slice.
