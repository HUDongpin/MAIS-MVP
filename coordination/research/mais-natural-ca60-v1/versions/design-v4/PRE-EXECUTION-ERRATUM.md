# MAIS Natural CA60 V4 pre-execution erratum

Date: 2026-08-25

Status: `DRAFT_OWNER_DECISIONS_PENDING / V4_REGISTRATION_NOT_FROZEN / EXECUTION_REGISTRATION_NOT_FROZEN`

This append-only erratum records a V4 candidate without rewriting V3 registration `08e89a4d0c6736b48c1dde0048d8f620c35fc486e1d1feaf272e30a1aae4973d`. V3 remains the ACTIVE design registration and has no provider execution. V4 proposes a future `SUPERSEDED_NOT_EXECUTED` disposition for V3 only after the owner decides the exact Qwen endpoint and data region, all V4 roots are recomputed, frozen-candidate validation succeeds, and independent review concurs. The exact 45-file V3 inventory is preserved by root `0aa952f44ee0558b0b5054b04bb669951e63e7c63a74a5a68881e70a3a5da471`; its registration file byte SHA-256 is `3299e95244540942ad82f6a616ace4d64be3a74ff1ba78172aeb6a34088f7939`.

The V4 candidate was assembled before any frame or sample freeze and before any provider event. Current evidence remains: no valid V4 registration freeze or root, no frame freeze, no sample freeze, no current provider authorization, no provider call, no machine-reference label, no natural-question result, and no independent review receipt. Provider event count is zero.

The candidate proposes to separate the design implementation baseline from the later-bound clean runtime source; adds closed evidence contracts for three-route parity, strict JSON item identity, full localized bundles, source manifests, rights, assets, failures, and lineage approval; binds exact provider logical/wire/raw/parsed evidence and pessimistic post-dispatch reserves; and defines a chronology in which nonzero Qwen reference history plus a separate non-natural DeepSeek route probe precede full DeepSeek authorization and execution registration.

The Qwen endpoint and data region remain `OWNER_DECISION_REQUIRED_NOT_FROZEN`; this blocks the V4 freeze. DeepSeek direct billing and data region remain `UNKNOWN_PENDING_ROUTE_PROBE_EVIDENCE`; full authorization is also blocked. Authorization text embedded in the candidate is only a template with `isCurrentAuthorization=false`.

Aggregate publication authorization is independently `BLOCKED_PENDING_A21_PROTECTED_CUSTODY_REGISTRY` because `EXECUTION_CUSTODY_REGISTRY_NOT_IMPLEMENTED`. This is not a second design-freeze decision blocker. Before publication can ever be authorized, an A21 protected runner must exact-pin the out-of-band custody registry for the execution, final, and review heads. The current review gate is a consistency validator only and is not publication authorization; caller-supplied roots cannot authorize export.

The overall decision ceiling remains `INCONCLUSIVE_MACHINE_REFERENCE`. The separate claim-scope ceiling is `CALIFORNIA_RUNTIME_EGRESS_ELIGIBLE_MACHINE_REFERENCE_PILOT_ONLY`. No `PASS`, general machine-QA validity claim, cross-region comparison, deployment, promotion, or live-content mutation is authorized.
