# Blocker Report

- Date: 2026-05-20
- Session ID: S11
- Task: Expand teacher console button and dropdown Playwright matrix.
- Blocker type: Scope conflict
- What happened: The expanded desktop teacher console matrix now covers the requested buttons, links, form submissions, downloads, status toggles, and dropdowns. It exposes four product-side defects outside S11's test-only scope:
  - Class creation, student add, live session start, and resource upload complete successfully but emit uncaught page errors: `Cannot read properties of null (reading 'reset')`.
  - Assessment creation returns 201 and redirects to `/teacher/assessments/{assessmentId}`, but that page renders the app 404 because the teacher assessment detail route is missing.
  - Report dropdown changes and preview refreshes complete, exports and save work, but the page emits repeated uncaught errors: `signal is aborted without reason`.
  - The resource upload/download workflow now passes its UI and download assertions, but still fails the no-page-errors gate because of the same async form reset issue.
- Files involved: `components/teacher/TeacherManagementViews.tsx`, `components/teacher/TeacherLiveView.tsx`, `components/teacher/TeacherResourceAssessmentViews.tsx`, `components/teacher/TeacherReportsView.tsx`, missing `app/teacher/assessments/[assessmentId]/page.tsx`, and test coverage in `tests/e2e/teacher-console-button-matrix.spec.ts`.
- Why the session stopped: S11 owns QA/E2E tests, not teacher feature code or route implementation. Fixing these issues requires S13 teacher console ownership and likely S12 route/API coordination for the assessment detail route.
- Decision needed from owner: Confirm whether S13/S12 should fix the teacher form reset errors, missing assessment detail route, and report abort handling before S11 converts the full desktop matrix into a required green release gate.
- Safe next step: Assign S13 to patch the teacher UI handlers to retain the form element before awaits, add/restore the assessment detail route, and handle report preview aborts without uncaught page errors; then S11 reruns `npx playwright test tests/e2e/teacher-console-button-matrix.spec.ts --project=desktop-chrome`.
