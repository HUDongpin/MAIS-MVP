All measurements are in. Nothing in this family was fixed — here is the report.

## Answer-key form/language family — re-measured against current main (`1112f9af33`), corpus 11,700

| Finding | Original claim (over 17,700) | Re-measured on main (11,700) | Verdict |
|---|---|---|---|
| **B4a — non-Chinese answer key** | 198 items display a Latin/English key (`sphere`, `cuboid`, `Quadrant IV`, `x=12 or x=17`, `3 R 5`) | **Exactly 198**, all in still-shipped slices: pep-primary 97, pep-junior 101, all other slices 0. Breakdown: `Quadrant IV` 34 (pep-junior S1), English solid nouns 52 (pep-primary — `cuboid` 15, `cube` 14, `cylinder` 13, `sphere` 8, `15 items` 2; 50 at P1, 2 at P6), `x=… or x=…` 67 (pep-junior S3), `N R M` 45 (pep-primary P2). Every one of the 86 original F6a ids, 67 F6b ids and 45 F6c ids still exists carrying a byte-identical key. | **SURVIVES** — zero attrition |
| ↳ severity sub-claim | `sphere` is not among the item's four options | **40 of 40** MC items in this class have a key that is **not one of the displayed options** (e.g. `pep-primary-p1-u-mc-051` key `sphere`, options 正方体/圆柱/球/长方体) | **SURVIVES** |
| ↳ the brief's split ("65 pep-primary, 67 pep-junior") | — | Wrong on both sides, and it is a transcription error in the brief, not a change on main: the source audit's own breakdown is pep-primary 97 / pep-junior 101, which is what I measure. | brief figure **dead** |
| **B4b — Latin unit on a 厘米 question** | 578 items (pep-primary 348, pep-junior 224, bnu-junior 5, hjb-primary 1) | Of the 578 original ids, **577 still exist**; the single loss is `hjb-primary-ds-v1-p6-235` (withdrawn slice). Independent strict re-scan (`answer` is exactly `<number><metric symbol>` and the prompt uses Chinese unit words): **504** — pep-primary 346, pep-junior 157, bnu-junior 1; 150 of them at P1–P3; 150 use the ASCII hack `cm^2`. A permissive rule reproduces 583, the extra bnu-junior hits being algebra answers where `m`/`t`/`s` are variables — false positives on both my scan and the original's. | **SURVIVES** (577/578) |
| ↳ grading impact | implied harm | All 504 strict-class items carry a Chinese-unit `acceptedAnswers` entry; **0** lack one. So a child typing `39厘米` is graded **correct** — the defect is display-only. The original never claimed otherwise, but the remediation plan should not price this as a grading bug. | **SURVIVES, narrower than it reads** |
| **B3 — 19 pep-high rounded-decimal keys** | 19 items keyed `-0.167`/`-0.333`; 1e-6 tolerance grades the exact fraction wrong; none has `acceptedAnswers` | All **19 ids present, identical keys, still zero `acceptedAnswers`**. An independent sweep of all 11,700 for rounded repeating decimals returns exactly those 19 and nothing else. Tolerance is still `Math.abs(...) < 0.000001` — now at `lib/server/answerMatching.ts:404` (audit cited :385; the constant is unchanged, the line moved). `\|−1/6 − (−0.167)\| = 3.333e−4 > 1e−6` confirmed in node. | **SURVIVES** |
| ↳ "4 of them instruct 优先使用精确值" | 4 prompts | **2**, not 4 (`pep-high-s6-rag4-sa-086`, `-091`). The original overstated this sub-claim. | **CHANGED (down)** |
| **Type-level cause** | `types/index.ts` has `answer: string` while prompt/options/explanation are `LocalizedText` | Verbatim on main: `prompt: LocalizedText` (1804), `options?: LocalizedText[]` (1805), **`answer: string` (1806)**, `explanation: LocalizedText` (1808). Untouched. | **SURVIVES** |
| **~90 confirmed-wrong keys** | ~90 items wrong/ambiguous by re-derivation | I enumerated every individually-named id across all six per-slice findings files: **134 ids**. **87 are in still-shipped slices and all 87 are present on main with byte-identical wrong keys** (pep-high 34, bnu-junior 25, pep-junior 19, hjb-junior 8, pep-primary 1). **47 are in withdrawn slices and all 47 are absent** from the shipped corpus (hjb-high 25, bnu-primary 12, hjb-primary 10). | **SURVIVES for 87 / MOOT for 47** |

### Spot-verification (12 re-derived in node, 12/12 still wrong)

`hjb-junior-ds-v2-s1-275` key `-6`, brute force gives max integer solution **−5**. `…s1-060` key `余式 9x-3`; long division gives **3x−3**, and reconstructing with the key's remainder yields `2x³−5x²+10x−1`, not the stated dividend. `…s2-186` key `k=1 或 -2`; both give α²+β²=**5**, not 7 (true roots (−1±√13)/2). `…s2-217` key `60/13`; area method gives **10/3**, and 60/13 is the altitude from B. `…s3-492` key `36`; population variance 31.222, closest option **32**. `…s3-447` key `7.27`; the 22 printed scores sum to 1583, variance **6.9525**. `bnu-junior-ds-v1-s1-188` key `25`; exhaustive search over u=0..9 gives the **empty set**. `…s3-089` key `12元或14元`; profit 640 at **12 or 16** (14 gives 720). `…s3-477` key `14岁，15岁`; median is **14.5**. `…s2-215` key `约900人`; 11/20 × 2000 = **1100**. The 11 SSS items `pep-junior-v2-s2-k06-sa-018…028` all violate the triangle inequality (4/23/34, 5/8/25, 6/11/27, 6/7/14, 8/8/17, 9/10/20, 10/12/23, 11/14/26, 12/16/29, 13/18/32, 14/20/35) — my measured set equals the claimed set exactly, 11/11. The three sampled rounded decimals all miss by 3.333e−4.

### How many of the ~90 are MOOT

**47 of 134 named ids (35%) are moot** — every one of hjb-high's 20 `AB·AC` dot-product items and 5 S4 key errors, bnu-primary's 9 三视图 items plus 3 singletons, hjb-primary's 6 multi-true MC items plus 4 singletons. **87 (65%) are live and untouched.**

But moot is not fixed, and the distinction matters here more than usual: `data/mainlandBnuPrimaryQuestions.ts`, `data/mainlandHjbPrimaryQuestions.ts` and `data/mainlandHjbHighQuestions.ts` are **still on disk and still compile**, their JSON packs still contain the defective ids (`hjb-high-ds-v2-s4-281…300` all 20 present, `bnu-primary-ds-v1-p3-091` present, `hjb-primary-ds-v1-p4-064` present). The withdrawal was done purely by dropping three spread entries from `curatedQuestions` in `data/questions.ts` — the remaining list at `data/questions.ts:2290-2296` still spreads bnu-junior, bnu-high, hjb-junior. Re-promoting any of those packs is a one-line change that returns every one of the 47 defects unmodified.

### What this means for the remediation plan

The plan's answer-key workstream is essentially undamaged and should be executed as written, minus a third of its item-fix list. Concretely: **B4 is fully intact — 198/198**, and it is the item to lead with, because 40 of those items show a child a "correct answer" that is not on the screen. B3 is intact at 19/19. The `answer: string` root cause is untouched, so the plan's step 9 ("promote `answer` to `LocalizedText`") is still the correct structural fix and is still a prerequisite for onboarding any non-Chinese locale. The only real re-scoping is arithmetic: the confirmed-wrong-key backlog drops from 134 to **87 items**, and the plan's step 10 ("re-solve bnu-junior and hjb-junior in full") is unaffected since both slices still ship.

Two corrections to carry forward so the plan is not built on inflated numbers. First, the 578 Latin-unit items are a **display** defect, not a grading defect — all 504 strict-class items already carry a Chinese-unit accepted answer, so this belongs in a presentation pass, not in the blocker queue alongside B3/B4. Second, the "4 prompts demand exact values then punish them" line is 2, not 4; keep the finding, drop the flourish. Finally, do not let anyone book the 47 moot defects as remediated — they are dormant in three compiling modules whose packs are byte-identical to what the audit condemned, and any decision to re-promote bnu-primary, hjb-primary or hjb-high must re-open those findings rather than inherit a green status.